# Production Compose Configuration
services:
  app:
    # Pull the image from the registry instead of building it natively.
    image: ${IMAGE_NAME:-ghcr.io/username/pkn-portal}:latest
    pull_policy: always
    env_file:
      - .env
    depends_on:
      pgsql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - app_code:/var/www/html
      - public_storage:/var/www/html/storage/app/public
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "php", "artisan", "about"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  nginx:
    image: nginx:1.27-alpine
    depends_on:
      app:
        condition: service_healthy
    ports:
      - "${APP_PORT:-80}:80"
    volumes:
      - app_code:/var/www/html:ro
      - ./docker/production/nginx:/etc/nginx/conf.d:ro
      - public_storage:/var/www/html/public/storage:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--spider", "http://localhost/up"]
      interval: 30s
      timeout: 5s
      retries: 3

  worker:
    image: ${IMAGE_NAME:-ghcr.io/username/pkn-portal}:latest
    pull_policy: always
    env_file:
      - .env
    depends_on:
      app:
        condition: service_healthy
      redis:
        condition: service_healthy
      pgsql:
        condition: service_healthy
    command: ["php", "artisan", "queue:work", "--tries=3", "--timeout=90", "--sleep=3", "--max-time=3600"]
    restart: unless-stopped

  scheduler:
    image: ${IMAGE_NAME:-ghcr.io/username/pkn-portal}:latest
    pull_policy: always
    env_file:
      - .env
    depends_on:
      app:
        condition: service_healthy
    command: ["sh", "-c", "while true; do php artisan schedule:run --no-interaction; sleep 60; done"]
    restart: unless-stopped

  pgsql:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${DB_DATABASE:-pkn_portal}
      POSTGRES_USER: ${DB_USERNAME:-pkn_portal}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-change-me}
    volumes:
      - pgsql_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  app_code:
  pgsql_data:
  redis_data:
  public_storage:
